{
  "name": "@breathprint/circuits",
  "version": "0.1.0",
  "private": true,
  "description": "Circom circuits for BreathPrint biometric ZK proofs",
  "scripts": {
    "build": "bash build.sh",
    "clean": "rm -rf build"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.4"
  },
  "devDependencies": {
    "circom_runtime": "^0.1.25"
  }
}
